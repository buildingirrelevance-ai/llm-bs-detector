# LLM BS Detector — .gitignore

# PowerShell sync scripts (local dev only)
*.ps1

# macOS
.DS_Store

# Windows
Thumbs.db
desktop.ini

# Editor directories
.vscode/
.idea/

# Zip packages (built locally, not committed)
*.zip
*.crx

# npm / node (not used, but just in case)
node_modules/
